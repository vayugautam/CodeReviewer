"""
models/__init__.py — placeholder for future ORM/database models.
Currently the app has no database; this package is reserved for when
one is added (e.g. SQLModel + PostgreSQL to store review history).
"""
